{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "packages/agent/Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'mkdir -p /app/packages/agent/data && cd /app/packages/agent && pnpm exec prisma db push --skip-generate && cd /app && node packages/agent/dist/core/cli-entry.js \"$FIXTURE_ID\" --persona=ilRegista,ilBomber,lUltimo'",
    "restartPolicyType": "ALWAYS",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "numReplicas": 1
  }
}
