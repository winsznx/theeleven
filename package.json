{
  "name": "regista11",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "web:dev": "pnpm --filter @regista11/web dev",
    "web:build": "pnpm --filter @regista11/web build"
  },
  "devDependencies": {
    "turbo": "^2.5.6"
  }
}
